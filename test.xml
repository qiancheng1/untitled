asf big w
w')
17 wopen.write(w_str)
18 fopen.close()
19 wopen.close()
hello
qianchne cheng
cheng
hello qianche big w
i hava some confuse

def log_config():
    logging.basicConfig(
        level=logging.DEBUG,
        filename="debug.log",
        filemode='w',
        format="%(asctime)s [%(lineno)d]   %(message)s"
    )